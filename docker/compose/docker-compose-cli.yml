version: '3.5'
services:
  php_cli:
    image: qazaq_genius/song_api/php_cli:dev
    container_name: qazaq_genius_song_api_php_cli
    working_dir: /var/www/html
    volumes:
      - ./../../code:/var/www/html
    environment:
      - VHOST=localhost